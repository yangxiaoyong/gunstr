'x' 与 "x" 是不同的，前者是一个整数，其值是字幕x在机器字符集中对应的数值（内
部表示值）；后者是一个包含一个字符（即字母x）以及一个结束符'\0'的字符数组

枚举常量是另一种类型的常量，枚举常量是一个常量整型值的列表
{{{
 enum boolean {NO, YES};
}}}

任何变量的声明都可以用 const 限定符限定。该限定符指定变量的值不能被修改
const 限定符也可以配合数组参数使用，它表明函数不能修改数组元素的值
{{{
int strlen(const char[]);
}}}
